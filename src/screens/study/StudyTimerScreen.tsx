import { memo, useEffect } from "react";
import { CommonType } from "../../types/common/CommonType";
import styles from "./styles/StudyTimerScreenStyles"
/**
 * 학습(타이머)화면입니다.
 */
const StudyTimerScreen = ({ route, navigation, appState }: CommonType.CommonProps) => {

    useEffect(() => {

    }, []);
    return (
        <></>
    )
}
export default memo(StudyTimerScreen);